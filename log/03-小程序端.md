# 小程序端（hospital-mini）进度



## Mock 机制

- 开关：`.env.development` → `VITE_USE_MOCK=false`（默认直连后端）
- 本地 Mock 代码保留：`src/utils/mock.js`（设 `VITE_USE_MOCK=true` 可恢复）

- 科室 composable：`src/composables/useDepartments.js`

- 图标映射：`src/utils/deptIcon.js`



## 页面与接口对照



| 页面 | 实际调用接口 | 状态 |

|------|-------------|------|

| 登录 | POST `/auth/login`、POST `/auth/wx-login` | ✅ |

| 首页 | departments, notice/list, register/list, payment/list | ✅ |

| 科室列表 | GET `/common/departments` + 下拉刷新 | ✅ |

| 科室详情 | departments + doctor/list | ✅ |

| 公告/就诊/缴费/我的 | 同前 | ✅ |



## 科室 API 完善（2026-06-29）



- `useDepartments` 统一四页（首页/列表/详情/就诊）数据源

- 科室列表：错误重试、候诊时长展示、`enablePullDownRefresh`

- 科室详情：常见疾病标签、候诊信息、加载态

- 首页：`featuredDepartments` 取 API 前 5 项，搜索仍用全量

## Mock 写库对齐（2026-06-29 全量扫描）

- `POST /register`：写入 `myRegisters` 并生成待缴 `myPayments`
- `POST /appointment`：写入 `myAppointments`
- cancel 接口：更新对应记录 `status = 3`
- `POST /auth/login`：同步 `his_user` / `his_token` 至 storage
- `GET /patient/info`：返回完整 `PATIENT_PROFILE` 字段

## Phase 3（2026-06-29）

- `request.js` 读取 `VITE_API_BASE`；`.env.production` 配置真机 API 域名
- `mpApi.wxLogin` → `POST /auth/wx-login`；`login.vue` 调用 `uni.login`
- `manifest.json` 增加 `networkTimeout` 60s

## 待办

- [x] 对接真实后端：`VITE_USE_MOCK=false`（2026-06-29 已切换）
- [x] 微信 OAuth 登录（2026-06-29 Phase 3）
- [ ] 生产环境：微信公众平台配置 request 合法域名



演示账号：`13800138000` / `123456`

## 三端最终检查（2026-06-29 · log/17）

- [x] `npm run build:mp-weixin` 成功 → `dist/build/mp-weixin`
- [x] visit 页拉取 `/common/register-types`
- [x] `POST /auth/wx-login` demo → 200
- [x] App.vue 页面上报 + request 写操作上报
