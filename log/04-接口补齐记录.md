# 接口补齐记录



> 记录每次扫描发现的缺口及修复情况，便于审计。



---



## 2026-06-29（完善）— 四项任务深度打磨



### 1. 用户端个人中心 `/patient/info`



| 完善项 | 说明 |

|--------|------|

| `composables/usePatientProfile.js` | 档案缓存 + 侧栏/个人中心/预约页复用 |

| `views/profile/index.vue` | 刷新按钮、错误重试、过敏/慢病 Tag、健康提示卡片 |

| `components/ProfileSidebar.vue` | 展示就诊卡号、脱敏手机号，静默预加载档案 |

| `mock/index.js` | `PATIENT_PROFILE` 常量，与登录 user 字段一致 |

| `stores/user.js` | `updateProfile()` 合并档案到本地 userInfo |



### 2. 用户端预约动态排班 UI



| 完善项 | 说明 |

|--------|------|

| `useAppointmentSchedule.js` | 排班错误态、重试、`scheduleSummary`、可用时段计数 |

| `views/appointment/index.vue` | 已选摘要 Tag、时段说明、档案姓名自动填充、排班失败重试 |

| 时段策略 | 仍以 `TIME_SLOTS` 常量为基准，与 API 可用性取交集 |



### 3. 小程序科室 API 化



| 完善项 | 说明 |

|--------|------|

| `composables/useDepartments.js` | 统一加载/错误/刷新/findById/findByName |

| `utils/deptIcon.js` | 按 sort 排序、`waitLabel()` 候诊文案 |

| `pages/department/department.vue` | 下拉刷新、错误重试、候诊信息展示 |

| `pages/department/detail.vue` | 候诊/常见疾病标签、加载态、encodeURIComponent |

| `pages/index/index.vue` | `featuredDepartments` 计算属性，搜索用全量 API 数据 |

| `pages/visit/visit.vue` | 共用 `useDepartments` |

| `pages.json` | 科室列表页 `enablePullDownRefresh` |



### 4. 全量 store enrich



| 完善项 | 说明 |

|--------|------|

| 新增 enrich | patients、services、drugs、finances、incomeExpenses、schedules、adminUsers |

| 增强 enrich | adminDepartments 确定性 waitingCount/todaySlots（去随机） |

| 覆盖集合 | 共 18 个 store 分区启动时补全 |



### 验证建议



```bash

cd hospital-user && npm run dev

# 个人中心刷新 / 侧栏卡号 / 预约选医生→日期→时段→已选摘要



cd hospital-mini && npm run dev:mp-weixin

# 科室列表下拉刷新 / 详情常见疾病 / 首页科室推荐



cd hospital-admin && npm run dev

# 药房/财务/患者列表字段完整性

```



---



## 2026-06-29（续）— 个人中心 / 动态排班 / 小程序科室 API / Store Enrich



### 已完成的四项任务（初版）



| 任务 | 变更文件 |

|------|----------|

| 用户端个人中心对接 `/patient/info` | `api/index.js`, `mock/index.js`, `views/profile/index.vue`, `stores/user.js` |

| 预约页动态排班 UI（静态 TIME_SLOTS 交集） | `composables/useAppointmentSchedule.js`, `views/appointment/index.vue`, `types/hospital.ts` |

| 小程序科室页改走 API | `utils/deptIcon.js`, `pages/department/*`, `pages/index/index.vue`, `pages/visit/visit.vue`, `utils/mock.js` |

| 全量 store enrich | `mock/helpers/storeEnrich.js`, `mock/data/store.js` |



---



## 2026-06-29 — 深度扫描 & 选择性补齐



（见上文及 `01~03` 模块日志）



---



## 模板（后续追加）



```markdown

## YYYY-MM-DD — 标题



### 发现缺口

- ...



### 已修复

- ...



### 遗留

- ...

```

## 2026-06-29 — 全量扫描 + 后端 Phase 1

### 发现缺口

- 小程序 `mock.js`：`POST /register` 不写库、cancel 不更新状态
- 无真实后端工程，三端均依赖 Mock
- 需求 PDF 五大模块 + 业务场景需对照验收

### 已修复

- **小程序 mock**：挂号/预约写库、cancel 状态更新、登录 storage、patient/info 完整字段
- **log/05-需求对照.md**：需求 PDF 模块矩阵
- **hospital-backend/**：Spring Boot 3.2 + MyBatis-Plus + JWT + schema/data.sql
  - H2 默认 profile 零配置启动（端口 8080）
  - MySQL profile 可选
  - 认证、公共字典、患者端全流程、管理端各模块 REST 对齐 Mock 路径

### 遗留

- ~~三端 `USE_MOCK=false` 切换与联调~~ ✅ 已完成
- ~~后端 Phase 2：复杂审批流、排班日历、微信 OAuth、Redis~~ ✅ 深度 + Phase3 已完成

## 2026-06-29 — 三端关闭 Mock 联调

### 已完成

- 三端 `.env.development`：`VITE_USE_MOCK=false`
- 后端契约修复：menu/tree、config、收支 summary/trace、报销/结算 detail
- 冒烟测试 18/18 通过（见 `log/06-联调记录.md`）

## 2026-06-29 — 深度回溯精细化部署

### 已完成

- 22+ 深度操作 REST 端点（排班日历/停诊、药品库存趋势、配药扫码、财务流水等）
- schema/data 种子扩展（barcode、phase、bank、revisions 等）
- `smoke-test-deep.ps1` 深度冒烟 10/10
- 对照文档 `log/07-深度回溯.md`

### 遗留

- ~~菜单 CRUD 内存树未持久化~~ ✅ Phase 3 已落库

## 2026-06-29 — Phase 3 后续

### 已完成

- `sys_menu` 持久化 + 43 条种子；`SysMenuController` CRUD 落库
- `sys_dict.remark` + `dictType` 过滤 + 11 条种子
- `income_expense.account_id` 关联；`getFlows(id)` 按账户过滤
- `CommonMetaService` + `@Cacheable`；redis profile 可选
- `POST /api/auth/wx-login` 演示模式；小程序 `login.vue` 对接
- `smoke-test-phase3.ps1` 6/6；合计冒烟 34/34
- 对照文档 `log/08-Phase3后续.md`

## 2026-06-29 — P3 安全审计

### 已完成

- BCrypt 密码（登录升级 + reset/create 加密）
- `@OperationLog` AOP → `sys_operation_log`
- `sys_role_menu` + 登录 `menuIds`
- `WxAuthClient` jscode2session + `patient.wx_openid`
- 收支 summary mom/7 日 trend 读库
- `smoke-test-p3.ps1` 6/6；全量 **47/47**
- 回溯文档 `log/10-P1P2回溯验证.md`、`log/11-P3完成回溯.md`

## 2026-06-29 — P1/P2 最后完善

### 已完成

- 统计 `analysis/reports/decision` 读库聚合（register/payment/inventory/bed）
- 科室 `/admin/department/tree` 返回嵌套 children
- `FinanceLedgerHelper`：支付/结算写流水并更新账户余额
- 报销/结算：`workflow_json`、`items_json`、`invoice_no` 持久化
- `smoke-test-p1p2.ps1` 7/7；合计冒烟 **41/41**

## 2026-06-29 — 收尾与部署

### 已完成

- 全量占位回溯；高价值深化（退款流水、admin 鉴权、mini bug、prod 配置）
- `execution/` 部署手册 + `database/hospital_his_full.sql`
- 文档 `log/12-收尾与部署.md`

## 2026-06-29 — 最后完善 P0

### 已完成

- `SysConfigHelper`：挂号费/专家费/预约天数读 `sys_config`
- `CommonMetaService`：options 优先 `sys_dict`（gender/pay_method 等）
- `SourceModuleLabels`：收支 trace/flows 来源标签与跳转路径
- `StatisticsServiceImpl.overview`：去掉 1286/86/342 假数；7 日就诊读挂号表
- `AppointmentServiceImpl.buildSchedule`：按排班日 + 已预约数计算 remaining
- 删除 `MenuDataProvider` 死代码
- 审计文档 `log/09-最后完善审计.md`

### 待办（P1/P2，见 log/09）

- 统计 analysis/reports/decision SQL 化
- 财务支付/结算写流水并更新账户余额
- 报销/结算 detail JSON 持久化
- BCrypt / 操作日志 AOP / 真实微信 OAuth

