# 最后完善 — 后端审计与深化清单

> 日期：2026-06-29  
> 对照：`log/00–08`、`需求陈述书-东软云医院HIS系统.pdf`、三端 Mock/页面  
> 原则：**不推翻骨架**，在现有 Controller/Service/表结构上深化或补齐预留占位

---

## 一、总体结论

| 维度 | 状态 | 说明 |
|------|------|------|
| API 面 | ✅ 齐 | 28 Controller、约 160 端点，Mock 100+ 路由已对齐 |
| CRUD 主链路 | ✅ 落库 | 26 Entity ↔ 26 表，MyBatis-Plus 全覆盖 |
| 深度操作 | ✅ 闭环 | 排班/药房/财务 22+ 端点（见 log/07） |
| Phase 3 | ✅ 完成 | 菜单/字典/流水/缓存/wx-login（见 log/08） |
| 占位/静态层 | ✅ P0–P3 + log/16 闭环 | 安全/审计/财务/统计/控制台均已深化 |
| 生产部署 | ⬜ 待配置 | 真实微信域名、MySQL init（见 log/16 README） |

**需求 PDF 七大模块**：管理端页面 + 后端 REST 均已覆盖；PDF 扩展项（微服务/ES）不在本次范围。

---

## 二、模块审计（后端 → 占位 → 最小深化）

### P0 — 配置与字典贯通（本轮已着手）

| 模块 | 现状 | 占位 | 深化动作 |
|------|------|------|----------|
| 挂号费率 | `RegisterServiceImpl` 硬编码 Map | 未读 `sys_config` | ✅ `SysConfigHelper` 读 `register_fee`/`expert_fee` |
| 预约天数 | 固定 8 天 | 未读 `appointment_days` | ✅ 读配置传入排班构建 |
| 公共字典 | `CommonMetaProvider` 静态 Java | 与 `sys_dict` 双源 | ✅ `CommonMetaService` 优先读 DB |
| 收支来源 | `IncomeExpense` getter 覆盖 DB 字段 | trace 显示错误 | ✅ `SourceModuleLabels` + 读库字段 |

### P1 — 统计与科室（✅ 已完成）

| 模块 | 深化结果 |
|------|----------|
| 统计 analysis/reports/decision | ✅ 读 `register_order`/`payment`/`inventory`/`bed` 聚合 |
| 统计 overview | ✅ P0 已去假数 |
| 预约 schedule | ✅ P0 已接排班 + 预约数 |
| 科室 tree | ✅ `DepartmentServiceImpl.buildChildren` 返回嵌套 `children` |

### P2 — 业务联动（✅ 已完成）

| 模块 | 深化结果 |
|------|----------|
| 支付入账 | ✅ `FinanceLedgerHelper` — pay/batchPay 写流水 + 更新账户 1 余额 |
| 结算入账 | ✅ settle 写流水 + 更新账户 3 余额 + `invoice_no` |
| 报销 detail | ✅ `workflow_json` / `attachments_json` 持久化；审批更新 workflow |
| 结算 detail | ✅ `items_json` 持久化；detail 返回 feeItems/unpaid 等 |
| 收支 summary mom | ✅ P3 读库计算 mom/trend |
| 患者详情/诊疗 chain | ⬜ 可选深化（非阻塞） |

### P3 — 安全与审计（✅ 已完成）

| 模块 | 深化结果 |
|------|----------|
| 密码 | ✅ BCrypt + 登录明文自动升级 |
| 微信 | ✅ `WxAuthClient` jscode2session + openid 绑定 |
| RBAC | ✅ `sys_role_menu` + 登录 `menuIds` |
| 操作日志 | ✅ `@OperationLog` AOP 写库 |

### 可删除

| 文件 | 原因 |
|------|------|
| `MenuDataProvider.java` | 已无引用，内容已镜像 `sys_menu` 种子 |

---

## 三、与 log 对齐

| log 文件 | 需同步项 |
|----------|----------|
| 05-需求对照 | Phase 3/菜单/Redis 描述过时 → 以 08 为准 |
| 06-联调记录 | 同上，补充 34 项冒烟 |
| 08-Phase3 | 菜单 CRUD 前端可深化 — 仍开放 |
| 00-总览 | 增加「最后完善 P0/P1」里程碑 |

---

## 四、与前端对齐

| 前端 | 后端占位影响 | 建议 |
|------|--------------|------|
| 管理端 dashboard | overview 假数与 Mock 一致 | P1 去假数后 UI 仍兼容（数字变小） |
| 用户端/小程序预约 | schedule 假余号 | P1 接 DB 后 remaining 更准确 |
| 管理端系统配置页 | 改配置不生效 | **P0 已贯通 register/appointment** |
| 管理端字典页 | 与 `/common/options` 不同步 | **P0 已优先读 sys_dict** |
| 小程序 wxLogin | 演示模式 | 生产前关 demo（log/08） |

---

## 五、执行顺序（非全量）

```
P0 配置/字典/来源标签  →  P1 统计真实化 + 排班余号  →  P2 财务/审批联动  →  P3 安全/审计
```

每步保持：**同一 Controller 路径、同一响应结构**，只替换内部实现或补字段。

---

## 六、完成记录

### P0（2026-06-29）

- [x] `SysConfigHelper` — 挂号费/预约天数读 `sys_config`
- [x] `SourceModuleLabels` — 收支 trace/flows 来源中文标签
- [x] `CommonMetaServiceImpl` — options 优先 `sys_dict`
- [x] `StatisticsServiceImpl.overview` — 去假数；7 日就诊读挂号表
- [x] `AppointmentServiceImpl.buildSchedule` — 排班日 + 已预约数
- [x] 删除 `MenuDataProvider`

### P1/P2（2026-06-29）

- [x] `StatisticsServiceImpl` — analysis/reports/decision 读库聚合
- [x] `DepartmentServiceImpl.tree` — 嵌套 children
- [x] `FinanceLedgerHelper` — 支付/结算写 `income_expense` 并更新余额
- [x] `Reimbursement` / `Settlement` — JSON 列 + detail/approve/settle 闭环
- [x] schema/data 扩展：`workflow_json`、`items_json`、`invoice_no` 等
- [x] `smoke-test-p1p2.ps1` — 7/7 通过

**冒烟合计**：basic 18 + deep 10 + phase3 6 + p1p2 7 + p3 6 = **47/47**

### P3（2026-06-29）

- [x] BCrypt / 操作日志 AOP / RBAC menuIds / Wx jscode2session
- [x] 收支 summary mom/trend SQL 化
- [x] `smoke-test-p3.ps1` 6/6
- 详见 `log/11-P3完成回溯.md`

### 生产待办

- [ ] 关闭 `hospital.wx.demo`、配置合法域名与 MySQL init
