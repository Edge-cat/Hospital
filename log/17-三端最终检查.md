# 三端最终检查 — 构建 + 接口 + 交付验证

> 日期：2026-06-29  
> **当前最新检查记录**（IDEA 移交前）  
> 环境：Docker MySQL 3307 + 后端 prod JAR 8080

---

## 一、检查结论

| 维度 | 管理端 | 用户端 | 小程序 |
|------|--------|--------|--------|
| 生产构建 | ✅ 13.4s | ✅ 15.0s | ✅ mp-weixin |
| Mock 默认关闭 | ✅ | ✅ | ✅ |
| 核心 API 联调 | ✅ | ✅ | ✅ |
| 操作上报 | ✅ | ✅ | ✅ |
| 患者 common 接口 | — | ✅ | ✅ |
| 可交付 IDEA 测试 | ✅ | ✅ | ✅ |

**总体：三端通过最终检查，可移交 IDEA / 第三方按 README 部署。**

---

## 二、生产构建

| 端 | 命令 | 产物 | base |
|----|------|------|------|
| hospital-admin | `npm run build` | `dist/index.html` | `/admin/` |
| hospital-user | `npm run build` | `dist/index.html` | `/user/` |
| hospital-mini | `npm run build:mp-weixin` | `dist/build/mp-weixin/` | 微信开发者工具导入 |

构建仅有 chunk 体积警告（>500KB），无错误。

---

## 三、后端冒烟（本次实测）

| 脚本 | 结果 |
|------|------|
| `hospital-backend/scripts/smoke-test.ps1` | **18/18 通过** |

覆盖：admin/patient 登录、meta、统计、管理端科室/菜单/配置、患者/医生/公告、挂号/预约/缴费/记录、财务摘要等。

---

## 四、三端关键 API（本次实测 7/7）

| 检查项 | 端 | 路径 | 结果 |
|--------|-----|------|------|
| 操作控制台 | 管理端 | `GET /api/admin/console/overview` | 200 |
| 操作日志 | 管理端 | `GET /api/admin/log/operation` | 200 |
| 号别费用 | 用户/小程序 | `GET /api/common/register-types` | 200 |
| 科室列表 | 用户/小程序 | `GET /api/common/departments` | 200 |
| 首页摘要 | 用户/小程序 | `GET /api/common/home-overview` | 200 |
| 医生列表 | 用户/小程序 | `GET /api/doctor/list` | 200 |
| 操作上报 | 三端 | `POST /api/audit/report` | 200 |
| 微信登录 demo | 小程序 | `POST /api/auth/wx-login` | 200 |

---

## 五、分端检查明细

### 5.1 管理端（hospital-admin）

| 项 | 状态 | 说明 |
|----|------|------|
| `.env.development` | ✅ | `VITE_USE_MOCK=false` |
| Vite proxy | ✅ | `/api` → 8080 |
| 操作控制台 Dashboard | ✅ | `consoleApi.overview` |
| 操作日志增强 | ✅ | source/client/path 筛选 |
| 前端上报 | ✅ | auditMap + operationReport + request 拦截 |
| 生产 base | ✅ | `/admin/` |

**演示账号：** admin / 123456 · 端口 5173

### 5.2 用户端（hospital-user）

| 项 | 状态 | 说明 |
|----|------|------|
| `.env.development` | ✅ | `VITE_USE_MOCK=false` |
| 患者接口 | ✅ | `/api/common/*`，不调 `/api/admin/*` |
| 在线挂号页 | ✅ | 已修复 register-types 500 误弹 Toast（`silent: true`） |
| 号别费用 | ✅ | 读 `/api/common/register-types`，失败本地兜底 |
| el-radio 号别 | ✅ | `:label` 符合 Element Plus 2.x |
| 操作上报 | ✅ | 路由 afterEach + 写操作拦截 |
| 生产 base | ✅ | `/user/` |

**演示账号：** patient / 123456 · 端口 5175

### 5.3 小程序（hospital-mini）

| 项 | 状态 | 说明 |
|----|------|------|
| `.env.development` | ✅ | `VITE_USE_MOCK=false`，`VITE_API_BASE=/api` |
| 挂号 visit 页 | ✅ | 启动拉取 register-types |
| 页面访问上报 | ✅ | `App.vue` onShow |
| API 写操作上报 | ✅ | `utils/request.js` |
| 微信 demo 登录 | ✅ | `HOSPITAL_WX_DEMO=true` |
| 构建产物 | ✅ | `dist/build/mp-weixin` |

**演示账号：** 13800138000 / 123456

---

## 六、本次会话修复项（用户端）

| 问题 | 原因 | 修复 |
|------|------|------|
| 进入「在线挂号」报错 | 旧后端无 register-types，axios 拦截器先弹错 | `silent: true` + 后端重启/IDEA prod |
| 号别单选异常 | `el-radio` 误用 `:value` | 改为 `:label` |

---

## 七、IDEA 移交说明

检查完成后 **8080 后端进程已停止**，避免与 IDEA `HospitalBackend (prod)` 端口冲突。

**MySQL（3307）保持运行**，IDEA 可直接启动后端 + 三端 dev：

```powershell
# 后端：IDEA 运行 HospitalBackend (prod)
cd hospital-admin && npm run dev   # 5173
cd hospital-user  && npm run dev   # 5175
# 小程序：npm run dev:mp-weixin → 导入 dist/dev/mp-weixin
```

完整步骤：根目录 `README.md` 文末、`hospital-backend/IDEA-启动说明.md`

---

## 八、log 索引

| 文件 | 内容 |
|------|------|
| 16 | 占位回溯、操作控制台、IDEA 部署基线 |
| **17** | **本文件：三端最终检查（最新）** |
